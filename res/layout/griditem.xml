<?xml version="1.0" encoding="UTF-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content" >

    <FrameLayout
        android:id="@+id/id_frameLayout"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content" >

        <com.tuku.main.MyView
            android:id="@+id/id_groupImage"
            android:background="@drawable/albums_bg"
          
            android:paddingLeft="20dip"
            android:paddingRight="20dip"
            android:paddingTop="18dip"
            android:paddingBottom="30dip"
            android:scaleType="fitXY"
            android:layout_width="fill_parent"
            android:layout_height="150dip" />

        <TextView
            android:id="@+id/id_groupNum"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/albums_icon_bg"
            android:gravity="center"
            android:layout_marginBottom="10dip"
            android:text="5"
            android:layout_gravity="bottom|center_horizontal" />
    </FrameLayout>

    <TextView
        android:id="@+id/id_groupTitle"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:layout_below="@id/id_frameLayout"
        android:layout_centerHorizontal="true"
        android:ellipsize="end"
        android:singleLine="true"
        android:text="Camera"
        android:textSize="16sp" />

</RelativeLayout>